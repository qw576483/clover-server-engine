# 服务端引擎修复记录（**S 编号体系**）

> ⚠️ **本文件是 `clover-server-engine`（服务端引擎）的编号体系，与客户端引擎的 E 号无关。**
> 客户端 `clover-client-unity-engine/修复记录.md` 的 `E1=Timer unscaled / E2=UIFactory / E3=TryGet`
> 是**另一套**含义；拿 S 号去查客户端、或拿 E 号去查服务端，都查不到东西。

改引擎 SOP 见全局 skill `patterns/engine-fix.md`：先最小复现（跑起来就红）→ 最小修复（不重构、不改公开签名）
→ 修完自证（引擎全量 build/vet + 复现用例修复前红 → 修复后绿 + 业务端到端复跑）。

**本轮共修 2 处：S1（网关会话清理越界 panic ⇒ 断线回调永不触发）、S2（引擎内建 master 房间协议号被业务号守卫拒绝）。**

---

## S1 · gwcore 会话清理切片越界 panic ⇒ 断线回调（OnDisconnect）永不触发（★ 影响所有用网关的项目）

**日期** 2026-09-20　**文件** `internal/transport/gateway/gwcore/session.go`

| # | 文件:行（修复后） | 改动 | 说明（为什么这么改 / 有没有动签名） |
|---|---|---|---|
| S1-a | `session.go:1290`（`cleanup` 摘账号索引） | 旧：`append` 截断后再写 `sessions[len(sessions)-1] = nil`「清尾」；新：**先在原长度上 `copy` + 清尾，再截断** | 原写法在「本会话是索引里唯一一条」时长度变 0 ⇒ `sessions[-1]` 越界 panic；且在多人同 owner 时清掉的是**最后一条存活会话** |
| S1-b | `session.go:1193`（`removeFromIDIndex`，on cleanup 路径） | 同上形状的同一修复 | ⚠️ 见下「为什么不能只修 S1-a」：**不修这处，断线回调依然不触发**（业务端到端栈已证） |

签名未动、调用点未动、错误处理未动，行为只朝正确方向变化。

### 缺陷本质

`cleanup(connID)` 的职责顺序是：摘会话 → **摘索引** → 关连接 → **派发断线事件**（`fireHardDisconnect`，函数尾部）。
两处索引摘除都用了同一个错 idiom：

```go
sessions = append(sessions[:i], sessions[i+1:]...)   // 长度已 -1
sessions[len(sessions)-1] = nil                     // “清尾”
```

`append` 之后切片长度已经变成 `n-1`，于是：

- **本会话是索引里唯一一条**（一个账号只有一个连接）⇒ `n=1,i=0` ⇒ 长度变 0 ⇒ 对 `sessions[-1]` 赋值
  ⇒ **`panic: index out of range [-1]`**；
- 即使长度不为 0，`sessions[len-1]` 清掉的是**最后一条存活会话**（同 owner 多端在线时把别人的索引项置为 `nil`）。

panic 发生在「派发断线事件」**之前** ⇒ 派发整段被跳过 ⇒ **`g.OnDisconnect` 注册的回调永不触发**，
现象就是「客户端断了，服务端 0 条业务断线日志」。真实链路里 panic 被 `createSession` 的 `safe.GoSafe`
兜住（只打栈、不停进程），所以是**静默失效**。

一句话：**清理路径的越界 panic 把紧随其后的断线事件派发整段吞了。**

### 为什么不能只修 S1-a（★ 端到端实测逼出来的结论）

`cleanup` 里有**两段**索引摘除，且在派发之前：

```
cleanup → （S1-a）摘 account 索引
        → removeExtraIDs → removeFromIDIndex → （S1-b）摘 "p:playerID" 等额外索引
        → 关连接 → fireHardDisconnect → OnDisconnect
```

只修 S1-a 后跑业务端到端，stderr 里仍然有 3 条 panic，栈是（原文）：

```
[util] SafeRun recovered panic: runtime error: index out of range [-1]
gwcore.(*Gateway).removeFromIDIndex(...)  session.go:1190
gwcore.(*Gateway).removeExtraIDs(...)     session.go:1238
gwcore.(*Gateway).cleanup(...)            session.go:1299
gwcore.(*Gateway).createSession.func1()   session.go:418
```

真实链路里 `"p:"` 索引**必然存在**（角色 playerID 由 `GWControlBind` 追加），所以 S1-b 是**同一缺陷的必经一段**，
不是"顺手多改一处"。两处修完，端到端 stderr 归零（见下）。

### 最小复现（修复前失败）

回归用例：`internal/transport/gateway/gwcore/session_cleanup_test.go`（3 条，含 1 条对照组）。

```bash
cd clover-server-engine
go test ./internal/transport/gateway/gwcore/ -run 'TestCleanup' -v -timeout 90s
```

**修复前原始输出（两段各自的红灯，均实测）：**

```
=== RUN   TestCleanupFiresOnDisconnectWithSingleIndexedSession
--- FAIL: TestCleanupFiresOnDisconnectWithSingleIndexedSession (0.00s)
panic: runtime error: index out of range [-1] [recovered, repanicked]
clover-server-engine/internal/transport/gateway/gwcore.(*Gateway).cleanup(...)
	C:/Work/Server/full-dev/clover-server-engine/internal/transport/gateway/gwcore/session.go:1280 +0x930
--- FAIL: ...
FAIL	clover-server-engine/internal/transport/gateway/gwcore	0.047s
```

```
=== RUN   TestCleanupFiresOnDisconnectWithSingleExtraIDIndex
--- FAIL: TestCleanupFiresOnDisconnectWithSingleExtraIDIndex (0.00s)
panic: runtime error: index out of range [-1] [recovered, repanicked]
clover-server-engine/internal/transport/gateway/gwcore.(*Gateway).removeFromIDIndex(...)
	C:/Work/Server/full-dev/clover-server-engine/internal/transport/gateway/gwcore/session.go:1190 +0x26c
clover-server-engine/internal/transport/gateway/gwcore.(*Gateway).removeExtraIDs(...)
	C:/Work/Server/full-dev/clover-server-engine/internal/transport/gateway/gwcore/session.go:1238 +0x13a
clover-server-engine/internal/transport/gateway/gwcore.(*Gateway).cleanup(...)
	C:/Work/Server/full-dev/clover-server-engine/internal/transport/gateway/gwcore/session.go:1299 +0x2be
--- FAIL: ...
FAIL	clover-server-engine/internal/transport/gateway/gwcore	0.037s
```

用例走**真实链路形态**：`New(Config{}, WithOnDisconnect(...))` + 一条已绑定 owner 的会话写进
`sessionShards` / `idShards`（与「登录回包提取 owner 后写入」的形态一致），再调 `cleanup`
（真实断连路径 `<s.bc.ClosedCh() → cleanup` 的同一个函数）。

### 修复后自证（带对照组）

```bash
cd clover-server-engine && go build ./... && go vet ./...        # 均全绿（exit 0）
go test ./internal/transport/gateway/gwcore/ -run 'TestCleanup' -v -timeout 90s
```

```
=== RUN   TestCleanupFiresOnDisconnectWithSingleIndexedSession
--- PASS: TestCleanupFiresOnDisconnectWithSingleIndexedSession (0.00s)
=== RUN   TestCleanupFiresOnDisconnectWithSingleExtraIDIndex
--- PASS: TestCleanupFiresOnDisconnectWithSingleExtraIDIndex (0.00s)
=== RUN   TestCleanupKeepsSiblingSessionsInIndex
--- PASS: TestCleanupKeepsSiblingSessionsInIndex (0.00s)
PASS
ok  	clover-server-engine/internal/transport/gateway/gwcore	0.031s
```

断言：① `cleanup` 不 panic；② `onDisconnect` 回调收到 `(connID, owner)`；③ `idIndex` 的 key 真被清空（不留僵尸索引）；
④ **对照组**：同 owner 两条会话时断开一条，索引里只剩另一条且没有 `nil` 空洞（证明旧「清尾」写错位置的问题一并消失，
而不是把旧行为换成另一种错法）。

**业务端到端（真起服 + 真关连接，详见文末「端到端复跑」）**：stderr **0 字节**（无 panic）；
`logic: 玩家掉线判负 …` / `logic: 断线清理 … running=true` 真的打出来了。

### 已知边界 / 精度限制

- **同型写法本轮修了 2 处（S1-a 在 cleanup 上、S1-b 在 cleanup 的必经子路径上），另有 2 处未改**（同一 idiom，位置在别的交互路径上，
  按「最小修复」只登记）：
  - `session.go:799`：同会话**换绑**到不同 owner 时解绑旧 owner 索引 —— 旧 owner 只有这一条连接时同样 `[-1]` 越界；
  - `gwcore.go:965`：`Kick`（多端互踢 / GM 踢人）摘除被踢会话索引 —— 同样条件同样越界。
  **修法与 S1 逐字相同**（`n := len(sessions)` → `copy` → `sessions[n-1] = nil` → `sessions = sessions[:n-1]`）。
  本轮未修原因：`Kick` / 换绑不在「断线回调」这条验收路径上，且改动范围已被任务书限定；**它们是同一缺陷未清的两条尾巴，建议紧接着清掉。**
- 断线事件的**触发时机**由配置 `disconnect_grace` 决定（本项目配 10s ⇒ 硬掉线晚 10s 触发）；本修不改变该语义。
- 本修只保证「清理不再中断」；断线后做什么（判负 / 保留座位 / 回收房间）仍是业务的簿记，引擎不代劳。

### 用法 + 首个消费方

业务无需改代码：断线事件由网关侧触发，业务只订阅：

```go
g.OnDisconnect(func(owner string) { ... })       // 硬掉线（最终清理）
g.OnSoftDisconnect(func(owner string) { ... })   // 软掉线（仅 disconnect_grace>0）
g.OnHardDisconnect(func(owner string) { ... })   // 等价 OnDisconnect
```

首个消费方：`clover-project-cr/server/game/logic/logic.go:113` 的 `g.OnDisconnect(l.onDisconnect)`
—— 修复前该回调**从未被调用**（「掉线判负 / 摘座位 / 回收空房 / 推大厅列表」整段不生效）；
修复后端到端日志实测三条都生效（下）。

---

## S2 · 引擎内建 master↔game 房间协议号被「业务消息号必须 > 10000」守卫拒绝（★ 影响所有用 room 跨节点寻主/接管的项目）

**日期** 2026-09-20

**文件** `internal/app/tcpmsg_bridge.go`、`internal/app/master_server.go`、`internal/app/facade.go`、
`internal/domain/room/master_handlers.go`、`internal/domain/room/pkgfacade.go`、`pkg/domain/room/README.md`

| # | 改动 | 说明（为什么这么改 / 有没有动签名） |
|---|---|---|
| S2-a | `tcpmsg_bridge.go`：`OnMsg` 拆出 `InternalOnMsg`（保留号内部路径，与 OnMsg 共用同一条 Logic 派发 + TCP handler 登记链路，只是不做 >10000 校验） | **业务号守卫原样保留**（`OnMsg` 里那句 panic 一字未改）；`OnMsg` 变成「校验 + 转调」，对业务行为完全不变 |
| S2-b | `master_server.go` / `facade.go`：新增 `MasterGame.InternalOnMsg`、`MasterGameFacade.InternalOnMsg`（pkg 签名 `func(uint32, event.Handler)`） | **新增**方法（供引擎内部包用），不改任何既有方法签名；facade.go 里加编译期断言钉住方法集 |
| S2-c | `internal/domain/room/master_handlers.go`：`MasterHandlerGame`（**internal** 接口）加 `InternalOnMsg`；`registerRoomMsg` 改调它 | 该接口业务不可见，实现者只有下面的桥接器；加方法不破坏业务 |
| S2-d | `internal/domain/room/pkgfacade.go`：新增**可选接口** `masterInternalRegistrar` + `wrapMasterCtx` 辅助；桥接器实现 `InternalOnMsg` | ⛔ 刻意**不往业务可见接口** `pkg/domain/room.MasterHandlerGame` 加方法（给已发布接口加方法 = 所有实现者编译失败）；宿主不支持时 panic 并给出定位信息 |
| S2-e | `pkg/domain/room/README.md`：MasterHandlers 段 | 文档同步：4 条协议号是引擎内建保留号、走内部路径；并写明业务侧要在 master 角色挂一次 + `Config.MasterCaller` 传 `g` |

### 缺陷本质

master 侧房间协议的 4 条消息号是**引擎内建**的：`EMasterRoomRegister=6001 / Unregister=6002 / Find=6003 /
TakeoverClaim=6004`（`pkg/shared/proto/msg.go` 引擎内部协议段）。

但 `room.NewMasterHandlers(mg).Register()` 原先经**业务注册门面**（`MasterGameFacade.OnMsg` → `tcpMsgBridge.OnMsg`）
注册，而门面里有给业务用户的硬约束「`msgID > InternalMsgMax(10000)`」——**引擎自己的内建号撞上了自己给业务的约束**。

一句话：**内建协议号没有内部注册通道，被迫走业务通道，被业务门槛拦下 ⇒ 注册期 panic，master 侧永远没有房间 handler。**

后果（业务侧实测）：`CallMaster(6001)` 同步等回包，master 回 `unknown msgID=6001`（或连接被掐），建房 handler 卡住不回包 ⇒
业务只能把 `room.Config.MasterCaller` 传 `nil` 绕开，**跨节点寻主/接管整条链路失效**。

### 最小复现（修复前失败）

回归用例：`internal/app/master_room_handlers_test.go`（3 条：注册 / **真 TCP 往返** / 对照组）。

```bash
cd clover-server-engine
go test ./internal/app/ -run 'TestMasterRoomHandlersRegisterInternalIDs|TestMasterRoomHandlerRoundTripOverTCP|TestMasterGameOnMsgStillRejectsInternalIDs' -v -timeout 90s
```

修复前原始输出：

```
--- FAIL: TestMasterRoomHandlersRegisterInternalIDs (0.00s)
panic: app: business message id must be > 10000; got 6001
```

（用户实际报的端到端形态就是这个 panic：`room.NewMasterHandlers(mg).Register()` 一调即崩。）

### 修复后自证（带对照组）

```
=== RUN   TestMasterRoomHandlersRegisterInternalIDs
--- PASS: TestMasterRoomHandlersRegisterInternalIDs (0.00s)
=== RUN   TestMasterRoomHandlerRoundTripOverTCP
--- PASS: TestMasterRoomHandlerRoundTripOverTCP (0.01s)
=== RUN   TestMasterGameOnMsgStillRejectsInternalIDs
--- PASS: TestMasterGameOnMsgStillRejectsInternalIDs (0.00s)
PASS
ok  	clover-server-engine/internal/app	0.108s
```

断言：① 4 条内建号全部注册成功 —— 既进 `Logic` 派发表（可被派发），也登记了 TCP handler（帧有入口）；
② **真 TCP 往返**：起一条 master TCP 通道 + 真客户端，用内建号 `6001` 走完
「Call → Logic 派发 → room handler → MasterRegistry.Register → 回包 OK」，
并断言 master 注册表里真能查到该 room 的 owner（`CallMaster` 的路径就是 `tcpmsg.Dial + Call`，见 `internal/app/game.go:464-485`）；
③ **对照组**：业务路径 `MasterGame.OnMsg(6001, ...)` **仍然 panic**，且文案仍是
`app: business message id must be > 10000` ⇒ **没有放宽对业务的硬约束**，只是给引擎自己开了内部路径。

### ★ 「`room.Config.MasterCaller` 能不能换回 `g`」——判据

**能**。判据（都不靠猜）：

1. **注册期不再被拒**：`TestMasterRoomHandlersRegisterInternalIDs` 断言 4 条内建号同时进了 Logic 派发表与 TCP handler 表；
2. **线上往返是通的**（这是关键，因为「注册成功」不等于「能派发」）：`TestMasterRoomHandlerRoundTripOverTCP`
   用真 TCP + 真客户端把 6001 走了一遍并断言回包 `OK=true` + master 注册表里查到 owner；
   源码级依据：master 的 TCP 服务端**没有号段白名单**（`internal/transport/tcpmsg/server.go:49,93,100`
   就是一张 `map[uint32]HandlerFunc` 直查），发送侧 `Game.CallMaster` 也只做 `Dial` + `cli.Call(msgID,…)`；
3. **唯一剩下的前提（业务侧要做）**：master 角色必须真的挂一次 `room.NewMasterHandlers(mg)`
   —— 没挂的话 master 只会回 `unknown msgID=6001`，那是「没人接线」，不是号段问题。
4. ⚠️ **未验证的部分**：跨节点（≥2 个 master/game 节点）的实际接管迁移链路本项目跑不了
   （`server_type: all` 单进程，没有第二个节点可迁）；本修只保证这条链路在**协议与注册层面**通。

### 已知边界 / 精度限制

- 业务**任何时候**都不能用 `OnMsg` 注册 ≤10000（含 6001..6004）；本修只给引擎内部路径开口。
- `InternalOnMsg` 与 `OnMsg` 走同一条链路，**不做额外校验**：调用方（引擎内部包）自己保证号段合法（当前唯一调用方 = room 的内建 6001..6004）。
- 观察到（本轮**未改**，登记备查）：`room.NewMasterHandlers(mg)` 构造时已 `Register()` 一次，业务若照 README 示例再显式
  `.Register()` 一次会**重复登记同 msgID 的 handler**（`Logic.InternalOnMsg` 只对「同一函数指针」去重，两次 `Register` 产生不同闭包）。
  单进程无实害（回包按 requestID 配对、`MarkReplied` 只认首次），属可清理的重复接线。
- **修好本缺陷只是必要条件**：业务侧还要 `MasterCaller: g` + 在 master 角色挂 `room.NewMasterHandlers(mg)`，跨节点寻主/接管才真的生效。
  引擎侧验到「注册不再 panic + 4 条 handler 就位 + 内建号 6001 真 TCP 往返成功」；
  **跨节点（多节点 master）的实际接管迁移链路未跑**（本项目单进程 `server_type: all`，没有第二个节点可迁）。

### 用法 + 首个消费方

```go
// game 角色：让房间外壳能跨节点寻主/接管
l.roomMod = room.NewModule(room.Config{
    MasterCaller: g,          // ← 修好后可以传 g（此前只能传 nil，整条链路跳过）
    Pusher:       func(pid string, msgID uint32, v any) error { return g.PushToPlayer(pid, msgID, v) },
    NodeAddr:     g.Addr(),
    Kernel:       myKernel,
})

// master 角色：挂一次内建房间协议 handler（内部走保留号路径）
app.Mount(app.RoleMaster, func(m *app.MasterGame) { room.NewMasterHandlers(m) })
```

首个消费方：`clover-project-cr/server/game/logic/logic.go:89` 的 `room.NewModule(...)`
—— 该处 `MasterCaller` 目前**刻意传 `nil`**，注释里第 ② 条「补上注册也不行：`room.NewMasterHandlers(mg).Register()`
被守卫拒绝并 panic」正是本缺陷。修复后该注释与传参需要更新（属业务项目改动，本片不越权修改）。

---

## 端到端复跑（引擎改了 ⇒ 业务必须重验）

环境：`clover-server-tools/windows-env/core/env.exe start`（MySQL 3306 / Redis 6379 / NATS 4222 已在监听）。
业务服务端 = **用修好的引擎重新 `go build`** 的二进制（`replace clover-server-engine => c:/Work/Server/full-dev/clover-server-engine`）。
驱动：复用 `clover-project-cr/.ai-tmp/test/driver/`（agent-03 留下的端到端驱动，未重写）+ 一次性 mid-battle 驱动。

**修复后（本轮）**：

- `clover-cr` 端到端驱动全链：`===== 结果：全部 PASS（36 项，其中 BLOCKED=0） =====`
  —— 含修复前被标 BLOCKED 的第 36 项「断线清理（摘座位 + 回收空房 + 通知大厅） B 的房间列表里该房已消失」。
- 服务端 **stderr = 0 字节**（修复前是 3 条 `index out of range [-1]` + 栈）。
- 「对局进行中关连接」一次性驱动 + 业务日志原行：

```
158: 2026-09-20T02:27:56.383+0800	INFO	logic/room.go:1195	【all】logic: 断线清理 player=p_cr_c_1789842462 room=r1789842466299-3 running=false
167: 2026-09-20T02:28:06.650+0800	INFO	logic/room.go:1195	【all】logic: 断线清理 player=p_cr_b_1789842462 room=r1789842462486-1 running=false
168: 2026-09-20T02:28:06.652+0800	INFO	logic/room.go:1195	【all】logic: 断线清理 player=p_cr_a_1789842462 room=r1789842462486-1 running=false
185: 2026-09-20T02:28:35.427+0800	WARN	logic/room.go:1192	【all】logic: 玩家掉线判负 room=r1789842505404-4 player=p_cr_mid_1789842505 seat=0
186: 2026-09-20T02:28:35.427+0800	INFO	logic/room.go:1195	【all】logic: 断线清理 player=p_cr_mid_1789842505 room=r1789842505404-4 running=true
```

（第 185 行 = 对局中硬掉线 ⇒ **座位 0 被正确判负**；第 186 行 `running=true` = 座位/房间簿记走完。
修复前这两行**一条都没有**。）

## 站点文档同步（`clover-doc/`，本片只读 ⇒ 只登记该改哪儿）

- `server/development/protocol-design.md`：消息号段表旁补一句「引擎内建号走内部注册路径（`InternalOnMsg`），
  业务 `OnMsg` 的 `> proto.InternalMsgMax(10000)` 硬约束不变；内建 master 房间协议号为 6001..6004」。
- `server/examples/room.md`：示例只写了 `room.NewModule(... MasterCaller: g ...)`，**缺 master 角色的
  `app.Mount(app.RoleMaster, func(m *app.MasterGame){ room.NewMasterHandlers(m) })`** —— 照现在的文档做，
  寻主/注册这条路走不通（master 侧没有 handler）。本轮已在引擎仓库 `pkg/domain/room/README.md` 里补上这条。
