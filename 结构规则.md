# clover-server-engine 结构规则

> 生效范围：`clover-server-engine` 全仓
> 目的：统一未来长期开发中的编码、分包、命名、依赖、暴露与评审规则，避免结构再次失控。
> 用法：本文不是理念说明，而是**长期有效的执行规则**。以后任何 agent / 人工在新增目录、编写代码、拆包、暴露 API、做 code review 时，都必须按本文逐条对照。

---

## 一、总原则

1. **先判断归属，再写代码。** 新功能落地前，先判断它应该归属哪个层级、哪个主域、哪个包。
2. **禁止“想到哪写到哪”。** 任何新目录、新包、新子系统都必须能解释清楚其结构归属。
3. **横切能力不准寄生在业务域里。** 能被多个域复用的能力，必须放在公共层，而不是直接长进某个业务目录。
4. **门面在 `pkg`，真身在 `internal`。** `pkg` **只做门面**（类型别名 / 变量转发 / 极薄参数适配），并且**允许** import `internal` 去指向真身；凡有行为 / 有状态的**实现体一律留 `internal`**。
   为什么必须是这个方向：`pkg/app` 等门面包**业务要能 import**，而业务（独立 Go module）受 Go `internal` 规则限制**进不去 `internal`** —— 唯一可行的接法就是让 `pkg` 当门面、指向 `internal` 的真身（规则全文见 §五）。
5. **长期清晰性优先。** 结构设计要为未来服务，不为短期省事牺牲长期秩序。
6. **不允许把“先放这里以后再说”当成正式方案。** 临时落点如果没有明确收口计划，就等于错误落点。

---

## 二、统一结构哲学

以后整个仓库只允许按以下 6 类来组织：

```text
internal/
  app/           # 装配、启动、进程入口
  foundation/    # 真基础底座（现存 config/ophttp；logger/metrics/trace 在 pkg/foundation；health/lifecycle 未实现）
  runtime/       # 引擎运行时原语（现存 globalstore（跨节点 KV 抽象）；timer/fsm/pool/ratelimit/async 在 pkg/runtime）
  transport/     # 连接、协议、转发、事件总线、跨节点通信 glue
  domain/        # 核心业务域：data/master/log/auth/mmo/room/object/sessiontoken
  shared/        # 纯工具、纯协议、纯算法、纯叶子能力
pkg/
  ...            # 门面层：类型别名 / 变量转发 / 极薄参数适配 + 自包含工具库（**允许** import internal；规则见 §五）
```

### 2.1 六类固定定义

| 类别 | 固定职责 | 不允许放入 |
|---|---|---|
| `app` | 启动、装配、挂载、bootstrap、生命周期接线 | 通用能力、工具函数、业务主逻辑 |
| `foundation` | 真基础底座 | timer/job/lock/watch/async 等运行时原语 |
| `runtime` | 运行时原语 | 业务语义、transport glue |
| `transport` | 协议、连接、投递、转发、路由、跨节点通信 | 业务决策 |
| `domain` | 业务主域 | 通用横切能力 |
| `shared` | 纯协议、纯工具、纯算法、纯叶子能力 | 生命周期、全局状态、业务语义 |

### 2.2 目录判定六问

新增包前，必须先回答下面六问之一：

1. 它是不是**装配入口**？→ `app`
2. 它是不是**基础底座**？→ `foundation`
3. 它是不是**运行时原语**？→ `runtime`
4. 它是不是**传输/协议/路由 glue**？→ `transport`
5. 它是不是**业务主域能力**？→ `domain`
6. 它是不是**纯共享工具/协议/算法**？→ `shared`

**如果答不清，就不准建包。**

### 2.3 新增目录硬规则

1. **优先新增子包，不优先新增顶层目录。**
2. 除非形成长期稳定的大类，否则禁止新增新的一级主域。
3. 新增目录必须满足以下三个条件才允许落仓：
   - 有明确职责
   - 有明确调用方
   - 有明确不该放进去的边界说明
4. 仅因为“当前方便”而新增目录，一律视为错误设计。

---

## 三、各层规则

## 3.1 app

### 允许放入
- 进程启动入口
- bootstrap / wiring / mount
- Game、Master、Gateway 等应用装配
- 生命周期挂载与模块注册
- 进程级配置组合与模块装载顺序定义

### 禁止放入
- 通用运行时能力
- 可复用业务逻辑
- 传输实现细节
- 新的工具函数集合
- 持久化实现
- 可独立复用的中间层组件

### 判定标准
如果一个能力脱离当前进程装配后仍能独立存在，那它就**不属于 `app`**。

### 常见错误
- 在 `app` 里写通用 helper
- 在 `app` 里写跨模块复用逻辑
- 在 `app` 里偷偷长 event bus / timer / lock 封装

### 评审口令
看到 `app` 新增 200+ 行通用逻辑时，优先怀疑落点错了。

---

## 3.2 foundation

### 允许放入
- `logger`
- `config`
- `metrics`
- `trace`
- `health`
- `lifecycle`
- `ophttp`

### 禁止放入
- `timer`
- `job`
- `lock`
- `watch`
- `async`
- `fsm`
- `pool`
- `ratelimit`
- 任何带明显调度、执行、状态推进语义的能力

### 判定标准
如果一个能力的核心问题是：
- 什么时候执行
- 如何重试
- 怎么并发运行
- 如何限流
- 如何持有锁

那它大概率不属于 `foundation`，而属于 `runtime`。

### 常见错误
- 因为“它比较底层”就塞进 foundation
- 因为“暂时没有更好位置”就挂进 foundation
- 在 foundation/rpc 里重复实现 breaker / retry / timeout 编排逻辑

### 评审口令
`foundation` 只允许放“真底座”，不允许放“会跑的东西”。

---

## 3.3 runtime

### 允许放入
- `timer`
- `job`
- `watch`
- `lock`
- `async`
- `fsm`
- `pool`
- `ratelimit`
- `circuitbreaker`
- 以后新增的运行时原语能力

### 禁止放入
- 业务流程判断
- 协议接入逻辑
- 网关/连接层 glue
- 只服务某一个业务域的局部能力

### 判定标准
如果一个能力解决的是：
- 任务如何调度
- 状态如何推进
- 并发如何隔离
- 锁如何获取
- 资源如何复用
- 请求如何限流

那它属于 `runtime`。

### 硬规则
1. `runtime` 是唯一合法的运行时原语层。
2. 同一种原语能力只能有**一个真相包**，禁止平行再起一套。
3. 不允许同时存在：
   - 两个 lock 根包
   - 两个 timer 主实现
   - 两个 ratelimit 主实现
   - 两套 job 调度主干
4. 如果某业务域需要 timer / lock / ratelimit，只能依赖 `runtime`，不能自带一套。

### 强提醒
- `timer` 和 `job` 必须同体系思考。
- `lock` 必须统一成唯一域。
- `watch`、`async`、`fsm`、`pool`、`ratelimit` 不准继续分散生长。

### 评审口令
看到任何“为了这个模块特殊需求所以自己实现一套 lock / timer / ratelimit”，直接判错。

---

## 3.4 transport

### 允许放入
- 连接管理
- 协议接入
- 编解码接入层
- 转发、路由、投递
- 跨节点事件总线
- 远程服务接入层
- 网关接入与多协议监听
- 连接到逻辑服 / master 的消息搬运

### 禁止放入
- 业务规则判断
- 领域状态变更策略
- 业务对象生命周期控制
- 具体玩法流程

### 判定标准
如果一个能力的核心问题是：
- 消息从哪里进来
- 消息往哪里发出去
- 连接怎么管理
- 节点之间怎么传
- 路由怎么选

那它属于 `transport`。

### 硬规则
1. transport 是 glue 层，不承载业务决策本体。
2. 业务域通过接口依赖 transport，不允许反过来把业务流程塞进 transport。
3. 不允许在 `master`、`gateway`、`event`、`net` 各自长期维护一套同类传输 glue。
4. 传输保障（replay / deadletter / reliable / dedup）如果本质上服务消息流，也应归 transport 视角，而不是混进业务逻辑层。
5. 服务发现的**注册与解析各角色只能有一套**：角色（logic / auth / log 等）统一走 `internal/app/discovery.go`
   （前缀 + 每实例唯一 key + 租约续租 / 轮询解析）。禁止 logic、auth、log 各自再写一套注册或解析；
   单 key 单值会互相覆盖，等于多实例失效。
   现状补充：另有两个**用途不同**的注册/发现文件——节点目录 `internal/app/node_registry.go` +
   `node_directory.go`（`clover/nodes/`，按节点 ID 找节点）与 master 分片发现 `internal/app/master_shard_discovery.go`
   （`clover/services/master/<index>`，按 key 定归属）；三套用途不重叠，但同样禁止再起第四套同类实现。

### 常见错误
- 在 transport 中读取/修改业务状态后直接做业务决策
- 在 domain 中新起一套跨节点消息投递
- 在 gateway 中硬编码特定业务玩法路由规则

### 评审口令
transport 只负责“把东西送到”，不负责“决定送什么”。

---

## 3.5 domain

### 当前主域
- `data`
- `master`
- `log`
- `auth`
- `mmo`
- `room`
- `object`
- `sessiontoken`

### 允许放入
- 与该域主线强相关的业务模型
- 域内状态组织
- 域内行为规则
- 域内专属子系统

### 禁止放入
- 通用 lock / timer / retry / ratelimit / bus
- 通用 transport glue
- 纯工具函数集合
- 其他域也会复用的横切能力

### 通用规则
1. `domain` 只承载真正业务主线，不承载横切原语，不承载通用 transport glue。
2. 每个域都要围绕单一主线组织，不准无限吸附横切能力。
3. 如果一个能力会被多个业务域复用，就不应该继续留在单一业务域内部。
4. 一个业务域内部如果已经出现“空间 / 同步 / 调度 / 传输 / 业务规则”五种语义混写，必须继续拆子域。

### 各域提醒

#### data
- 主线应围绕 store / schema / query / record / session 等数据能力。
- 不允许把通用分布式锁、通用缓存算法、通用消息同步协议长期放在 data 内部。

#### master
- 主线应围绕 authority / state / rank / failover / discovery。
- （选主 / leader 选举已整体移除；多节点方向已落地**按 key 分片**方案——`internal/domain/master/config.go` 的
  `ShardConfig`、`internal/domain/master/client/sharded.go` 的 `ShardedClient`、
  `internal/app/master_shard_discovery.go`，详见 `修复记录.md`；重做选主类方案时再回来更新本条。）
- 不允许把远程接入协议、tcp 服务实现、lock 远程接入长期混在 master 主域里。

#### auth
- 主线应围绕**凭据与登录规则**：注册、登录、JWT 签发与验签、渠道票据换账号、撞库防护。
- 权威数据是账号表与渠道绑定表（属 data 平面，`domain/data/account`）；本域只管凭据，不碰玩家/游戏数据。
- 传输形态：**对客户端只开 HTTP**（登录是低频请求-响应，且要与渠道 SDK / 运营后台等 HTTP 世界交互），
  所以凭据链路的线协议契约是「路径 + 请求/响应载体」而非消息号。
  业务消息另有 `auth.rpc_listen` 消息通道（与 master / log 对称，走 `OnMsg`），**不承载登录校验**。
- 调用侧（game 校验凭证）在 `auth/client`，实现 transport 定义的 `Authenticator` 接口 ——
  「业务域通过接口依赖 transport」的范例。登录流程不许再塞回 transport。
- 业务注入点只有渠道票据校验器（`ChannelVerifier`），注册入口在 `pkg/app`；
  账号体系的扩展点在账号服，别再往游戏服加能接管登录链路的平行实现。
- 撞库防护状态目前是**进程内**的：账号服多实例横扩时各算各的。需要全局一致时按域内规则替换实现。

#### log
- 主线应围绕**日志接收与落盘**：game 侧 ringbuf 攒批（`log/logbuf`）→ 自有 TCP 报文（`state/wire.go`）
  → `state.LogService.WriteBatch` 落 `biz_log`。
- 日志条目的类型真身在 `pkg/foundation/logbuf`（业务经 `g.AddLog(..., logbuf.WithLevel("info"))` 写）；
  缓冲器本体与装配留在域内 / `internal/app`，**不在 pkg 暴露**（业务不 new、不持有）。
- 落盘后端**可配置、可注入**：契约真身在 `pkg/foundation/logstore`（业务方据此实现自己的后端），
  引擎内置 MySQL（`state/mysql.go`）为默认值。业务经 `app.RegisterLogBackend(name, factory)` 注册，
  再用配置 `log_backend` 切换 —— **换后端改配置即可，不改引擎代码**。
  这是硬约束：不许把「换落盘后端」做成需要业务方 fork 引擎才能办到的事。
- log 服**无状态、可横扩**（多实例写同一张表，经 etcd `log` 前缀被发现；同步 RPC `CallLog` 与业务日志
  管道 `AddLog` 两条通路都按该前缀做发现与轮询分发）——所以不许在域内引入跨实例协调状态。
- game 侧的日志分发只能**复用** `internal/app/discovery.go` 的解析器（`logbuf` 的 `Dialer` 由 app 注入，
  域内只负责「每实例一条长连接 + 按片轮询 + 失败弃连接重连」）：不许自建第二套发现 / 寻址逻辑
  （呼应本节「服务发现只能有一套」）。
- 不许把 log 服当事件总线 / 消息中间件用（跨节点投递归 transport 的 event / reliable），
  也不许把通用攒批 / 重试 / 限流算法堆进 log。

#### sessiontoken
- 主线应围绕**跨节点共享的会话凭证**（玩家断线重连凭据）这一件事，别再往这个域塞通用缓存。
- 分工：**权威在 master**（`master_session_token.backend`：memory / redis）；本域是 game 侧的
  **远程优先 + 降级兜底**——先问 master，只有**网络错误**才回退到本地 `sync.Map`（本地 token 带 TTL）。
- 为什么必须跨节点：玩家在 A 节点登录拿 token，断线后可能重连到 B 节点，B 必须能校验。
  所以**不许为省一跳把它改成进程内**，也不许在 game 里再长第二套 token 存储。
- 失效只有三种：重新进入游戏（覆盖旧 token，旧值立即失效）、显式登出、TTL 到期；
  **普通断网不删 token**（这正是留给重连的）。
- 已知边界：master 正常回 `valid=false`（如重启后）时本地兜底**不生效**（兜底只在网络错误时触发）
  ——这也是生产建议 `master_session_token.backend: redis` 的原因。

#### mmo
- 是最容易变胖的域。
- 采用**场景 / 实例切图**模型：一个 World = 一张地图，多 World = 分线，跨 World 通过切场景（带加载）实现。不做无缝大世界。
- 后续新增能力必须先判断属于：
  - spatial
  - gameplay
  - sync
- 不准把空间、战斗、同步、迁移、路由随手混写进同一层。
- 禁止重新引入分片编排类子域（`zonemap` / `partition` / `orchestration/*` 已删除）。跨机对象迁移（`TransferRemote` / `HandleRemoteTransfer`）属切场景基础能力：原语在 `internal/domain/mmo/scenemanager.go`，跨节点传输入口在 `internal/transport/remotexfer.go`，**接线（路由登记 / 续期 / 注销 + 接收端订阅）在 `internal/domain/mmo/cluster.go`**。
  这条链路的装配依赖全局 KV 与配置 `node_id`，**单机不配即不启用**（`TransferRemote` 返回 `ErrNoRoute`）—— 改它时别把这个降级行为破坏掉，也别把「发出去没人收」的静默失效重新引回来（历史上就踩过：发布端齐了、接收端零调用）。

#### room
- 主线应围绕**房间外壳**：房间生命周期、owner 归属、跨服 takeover。
- 房间内部「怎么同步」由**可插拔内核**决定（`pkg/domain/room.Kernel`）：
  引擎提供帧同步内核；状态同步等由业务自写内核经 `room.Config.Kernel` 挂入。
  **不为每种同步方式新开子域，也不把同步策略写进外壳。**
- 外壳不得解析内核状态（接管搬运的状态一律以不透明字节承载），否则换内核就要改外壳。
- 不要把 generic transport / generic retry / generic timer 继续堆进 room。

#### object
- 主线应围绕对象模型、对象存储、对象行为承载。
- 不准因为 object 当前较干净，就顺手往里面塞别的通用能力。

### 评审口令
domain 里只能长“这个域独有的问题”，不能长“全仓都会遇到的问题”。

---

## 3.6 shared

### 目录结构

```text
pkg/shared/          # 对外共享（业务可 import）
  bitset/            # 位集合
  bloom/             # 布隆过滤器
  cache/             # 通用缓存
  compress/          # 压缩（flate 封装）
  conv/              # 类型转换
  geom/              # 几何计算
  graph/             # 图算法（A* 搜索内核）
  hyperloglog/       # HyperLogLog 基数估算
  id/                # ID 生成
  json/              # JSON 工具
  jwt/               # JWT 签发与校验
  memrank/           # 内存排行
  proto/             # 对外协议门面（消息号真身）
  rand/              # 随机数（加权抽样 / 随机源）
  ringbuf/           # 环形缓冲区
  safe/              # 安全操作（并发安全 map 等）
  semaphore/         # 信号量
  timeutil/          # 时间工具
  timewindow/        # 时间窗口
  traceid/           # TraceID 生成
  util/              # 通用工具（util.go：哈希 / 切片函数）
  validate/          # 校验工具

internal/shared/     # 引擎内部复用（业务不可 import）
  config/            # 配置解析
  proto/             # 内部信封 / 协议真身的引擎侧复用
  retry/             # 重试策略
  tlsutil/           # TLS 证书加载 / 自签生成等纯工具
```

### 允许放入
- 纯工具
- 纯协议
- 纯算法
- 无状态叶子能力
- 可跨域复用但不携带框架生命周期的能力

### 硬规则
- **所有子包平铺为一级目录；`util/` 只作为普通一级子包存在（内部不再嵌套下一层子目录），
  禁止把其它子包聚合进统一 `util/` 包。**（历史表述「禁止嵌套 `util/` 子目录」与现状——
  `pkg/shared/util/` 已是独立一级子包目录——已不相符，按现状收敛为本条。）
- 每个子包保持独立，不聚合进统一的 `util/` 包。
- `pkg/shared/util/` 仅保留跨子包共享的通用工具（`Fnv32` / `Xxhash64` / `NextPow2` / `Contains` / `Equal` 等，实现文件为 `util.go`）。

### 禁止放入
- 半框架能力
- 需要全局状态的能力
- 带业务语义的能力
- 带装配逻辑的能力
- 带长期后台运行行为的能力

### 判定标准
如果一个能力满足以下全部条件，才允许进入 `shared`：

1. 不依赖业务域状态
2. 不依赖运行时生命周期
3. 不控制连接/协议/路由
4. 不要求全局单例
5. 可以被多个域直接复用

### 硬规则
- `shared` 不是新的垃圾桶。
- 它不是“暂时不知道放哪就放这里”，而是“明确可跨域复用、且不携带框架状态”的能力才允许进入。
- 不满足纯叶子条件的，一律不得进 `shared`。

### 常见错误
- 把半成品框架模块塞进 shared
- 把业务通用但有状态的 manager 放进 shared
- 把以后可能会变复杂的能力提前扔进 shared 占位

### 评审口令
shared 只允许放“拿来就用、不需要养”的东西。

---

## 四、编码规则

## 4.1 包设计规则

1. **一包一主线。** 一个包必须有单一职责，不能同时承担多种明显不同语义。
2. **优先做子包，不优先做顶层目录。** 除非确实形成新的稳定主域，否则不要新增顶层主域。
3. **薄包要谨慎。** 只有 1~2 个文件、且没有清晰扩展方向的目录，优先合并，不要随手新建。
4. **空目录不保留。** 不做“未来可能要用”的占位目录。
5. **孤儿代码要清理。** 长期无人引用的实验实现，要么接入主干，要么删除。
6. **目录一旦建立，就必须有清晰边界。** 不允许靠约定俗成猜职责。

### 包设计自检清单

新增包前必须自问：

- 它的主职责是什么？
- 它的调用方是谁？
- 它不该承载什么？
- 仓库里是否已有近似能力？
- 如果已有近似能力，为什么不是扩展原包？

答不出来，就不要建。

---

## 4.2 命名规则

1. **目录名必须表达职责，不表达历史来源。**
2. 禁止出现同名不同义或同义不同名长期并存。
3. 同一种能力只允许一个稳定命名，不允许为了局部方便额外起别名目录。
4. 缩写只在行业约定非常稳定时使用；否则优先全称。
5. 一旦发现脏命名，应尽快收敛，不要继续扩散。

### 命名禁止项

- 不允许出现 `rank` / `rankmodule` 这种一眼看不出边界差异的双名并存
- 不允许出现 `metric` / `metrics` 这种历史分裂命名长期共存
- 不允许因为不同实现手法，就给同一职责起两个不同的顶层包名
- 不允许在新目录上沿用已经决定废弃的旧术语

### 命名评审口令
命名必须回答“它负责什么”，而不是“它是怎么来的”。

---

## 4.3 依赖方向规则

1. **门面方向（铁律）**：`pkg` 只做门面（别名 / 转发 / 极薄适配），**允许** import `internal` 去指向真身；`internal` 是实现所在层。
2. **`internal` 禁止 import `pkg` 的业务包**（那会与门面方向构成环）；只允许 import **自包含包**（自身不 import internal 的类型真身 / 工具库，如 `pkg/shared/**`、`pkg/foundation/**`、`pkg/runtime/**`、`pkg/domain/mmo/<子包>`、`pkg/transport/event`，见 §5.2）。
3. `foundation` 不依赖 `domain`。
4. `runtime` 不依赖 `domain`。
5. `shared` 不依赖业务域语义。
6. `transport` 不直接承载业务决策；业务通过接口接入 `transport`。
7. 如需跨层调用，优先抽接口，不要直接硬连具体包。
8. 不允许为了图快跨层 import 一个实现包，然后把分层规则打穿。

### 依赖方向判定口诀

- `app` 可以依赖所有下层，但只负责装配
- `foundation` 只能向更基础的 shared 依赖，不向 domain 倾斜
- `runtime` 只能依赖 foundation/shared，不依赖业务域
- `transport` 可以依赖 runtime/foundation/shared，但不写业务决策
- `domain` 可以依赖 runtime/transport/foundation/shared，但不能反向污染这些层
- `internal` 是实现层；`pkg` 是门面层（别名 / 转发 / 极薄适配 + 自包含工具库）。
- **门面方向**：`pkg` 可以 import `internal`；`internal` 只允许 import `pkg` 的**自包含包**（§5.2）。

### 依赖评审口令
看到 `internal/...` import 一个 `pkg` 包时，先问一句：**这个 pkg 包自己 import internal 吗？**
import = 它是门面包 ⇒ **判错**（成环，`go build` 会直接报 import cycle）。
不 import = 它是自包含的类型真身 / 工具库 ⇒ 允许（§5.2）。

---

## 4.4 复用规则

1. 发现已有同类能力时，先收敛到原能力，不准平行再起一套。
2. 发现同类实现多套并存时，必须明确谁是唯一真相包。
3. 新功能落地前，先搜索仓库里是否已经存在同类能力。
4. 如果已有能力不够用，优先扩展原实现，而不是新建并行实现。
5. 不允许因为“这个模块有点特殊”就重新发明一套基础能力。

### 必查能力清单

写新代码前，至少先确认仓库里有没有现成的：

- lock
- timer
- job
- ratelimit
- metrics
- retry
- config
- session
- cache
- codec
- event bus
- pool
- fsm

### 复用评审口令
只要不是明确的新语义，就默认应该复用老能力，而不是新建一套。

---

## 五、pkg 与 internal 关系规则

> **现行方向：门面在 `pkg`，真身在 `internal`。** 取代 2026-09 那版「类型真身在 pkg、`pkg` 禁止 import internal、
> 零别名」——那一版与**现实冲突**：业务模块是独立 Go module，受 Go `internal` 规则限制，**进不去 `internal`**，
> 而 `pkg/app`（业务唯一的装配入口）必须能被业务 import ⇒ **门面方向是唯一可行方向**。
>
> **⚠️ 实测尚未全量达标**：`pkg/**` 里仍有部分包留着类型真身与整段实现。
> 它们分两类，**评审时逐包判，不按名单**：
> ① **自包含包**（自身不 import internal）—— 如 `pkg/shared/**`、`pkg/foundation/**`、`pkg/runtime/**`、
>    `pkg/domain/mmo/<子包>`、`pkg/domain/data/**`、`pkg/transport/event`、`pkg/domain/room/**`。
>    §5.2 允许（类型真身 + 工具库整段实现都可以留）；
> ② **门面包**（自身 import internal）—— 只允许有别名 / 转发 / 极薄适配，**不许有实现体**。
>    当前业务域门面包（`pkg/app`、`pkg/domain/mmo`、`pkg/domain/object/{gobject,objstore}`）已按 §5.1 收敛。
> 自查（现场测，不查名单）：
> `grep -rn "github.com/qw576483/clover-server-engine/internal" pkg` —— 命中是**门面包**还是**自包含包**，逐个判。

### 5.1 依赖方向（铁律）

1. **门面方向。** `pkg/**` 只允许做门面：
   - **类型别名**：`type X = internal.X`（业务 F12 一跳到位，编译期是同一个类型）；
   - **声明转发**：`var F = internal.F` / `const K = internal.K`（函数、变量、常量都走这一形式）；
   - **极薄参数适配**：把门面接口 / 值适配成 `internal` 具体类型、把 `internal` 返回类型协变包装成门面接口 ——
     **1~5 行、不加逻辑、不改语义**，且必须在同一个文件里紧邻说明它为什么存在。
2. **`pkg/**` 可以 import `internal/**`。** 这不是例外，这就是门面的**定义**（门面必须指向真身）。
3. **`pkg/**` 禁止放实现体。** 有行为 / 有状态的东西（函数体、方法体、算法、装配、生命周期、连接管理、
   存储实现）真身必须在 `internal/`；`pkg` 侧同名符号只能是别名或转发，**同一个符号不许 pkg 与 internal 各有一份实现**
   （两份必然漂移）。**类型声明**（struct 字段、接口、枚举、错误变量）允许留在 `pkg` —— 它是契约，不是实现。
4. **`internal/**` 禁止 import `pkg/**` 的业务包**（会把门面方向反转成环）。
   **例外只有自包含包**：`pkg/shared/**`、`pkg/foundation/**`、`pkg/runtime/**`、`pkg/transport/event`、
   `pkg/domain/mmo/<子包>`、`pkg/domain/data/**`、`pkg/domain/room/**` 等**自身不 import internal** 的
   类型真身 / 工具库包（§5.2 允许）。
5. **判环公式（评审直接用这一条）**：若 `pkg/A` import `internal/B`，则 `internal/B`（含其全部依赖）
   **不得** import `pkg/A`。违反 ⇒ `go build` 直接报 `import cycle not allowed`，不用讨论。

### 5.2 pkg 允许的内容

判据只有一句：**这个包自己 import internal 吗？**

| 它自己 import internal？ | 它是什么 | 允许放什么 |
|---|---|---|
| **是** | **门面包** | ① 类型别名；② 声明转发（`var` / `const`）；③ 极薄参数适配（§5.1 第 1 条）。**实现体禁止** |
| **否** | **自包含包** | 类型真身（struct / 接口 / 枚举 / 错误）+ **整段自包含实现**（纯算法 / 纯数据结构 / 工具库）+ 面向业务的稳定接口与构造函数 |

自包含包的清单（现状，非封闭枚举）：`pkg/shared/**`、`pkg/foundation/{logger,logbuf,logstore,metrics,trace}`、
`pkg/runtime/{timer,fsm,pool,ratelimit,async,watchdog}`、`pkg/domain/mmo/{ai/btree,aoi,buff,collide,combat,mapdata,mob,mover,pathfinding,skill,sync}`、
`pkg/domain/data/{types,schema,data,account,player,order}`、`pkg/domain/room/**`、`pkg/domain/master`、`pkg/transport/event`、
`pkg/app/types`。

**唯一破环例外**：P3 注册钩子（`RegisterXxxFactory` 声明，函数体不出现 `internal.`）—— 见 §5.5 F4。

### 5.3 pkg 禁止的内容

- **实现体**：函数 / 方法体里真正干活的代码（装配、生命周期、连接管理、存储实现、算法、遍历、状态机）。
  门面包里出现这些 ⇒ 落点错了，搬去 `internal`，`pkg` 侧留别名 / 转发。
- **同一符号 pkg 与 internal 双份实现**（必然漂移。实测代价：`pkg/app.GatewayConfig` 曾是门面自己的一份 struct，
  漂移成缺 `AuthDisabled` / `AuthExemptMsgIDs` —— 业务按门面类型根本设不了登录门禁；现已改为别名）。
- **门面之外**的 `internal` 依赖：`pkg` 引用 internal 只能是为了「指向真身 / 适配真身」，
  不许拿 internal 的东西在 pkg 里**做出一层新逻辑**。
- 临时迁移逻辑、只给引擎内部用的中间态 API、未稳定的实验性接口。

### 5.4 暴露判据：业务将来会不会用

对 `pkg` 每一个导出符号问：**业务（含将来）会亲手 new / 持有 / 点到它吗？**

- **业务语义** → 留，哪怕现在没人调。
- **引擎装配 / 生命周期 / 连接管理** → 删出 pkg，哪怕引擎自己正在调。
- 引擎自己在调 ≠ 业务要见到；引擎内部用 `internal` 即可，别抬进 pkg。

### 5.5 四种门面形态（pkg 与 internal 怎么接）

按「真身是什么形状」选，**先 F1，F1 不行再往下**：

| 形态 | 适用 | 做法 |
|---|---|---|
| **F1 纯别名 + 转发** | 真身在 internal，且门面签名与真身**逐字一致** | `type X = internal.X`；`var F = internal.F`；`const K = internal.K`。**首选，零适配、零漂移** |
| **F2 协变包装** | 门面是**接口**、真身是**具体类型**（真身方法返回具体类型，接口要的是接口） | internal 侧定义 `XxxFacade` 接口 + `xxxImpl` 包装（把 `*内部类型` 协变包成门面接口）；`pkg` 侧 `type Xxx = internal.XxxFacade`。**接口不要和内部同名 struct 撞名，加 `Facade` 后缀** |
| **F3 极薄参数适配** | 门面接口 / 值与真身参数**形状不同**（如门面 `data.Store` 要解包成内部 `*data.Store`） | 门面侧 1~5 行适配，**不改语义、不加逻辑**；留 `From*` / `Internal*` 两个方向便于还原 |
| **F4 注册钩子** | 业务必须**直接 new**、而实现必须留 internal | `pkg` 只声明 `RegisterXxxFactory`（函数体不出现 `internal.`），`internal` 在 `init()` 里注册（database/sql 式） |

**门面上移的判据**（旧版遗留）：真身是自包含工具（自身不 import internal）⇒ 它留在 `pkg` 就是自包含包（§5.2），
不需要「上移 / 下移」；只有**带 internal 依赖的实现**才必须搬进 `internal`。

### 5.6 类型家留在 `pkg` 的判据（含本轮结论）

「内部实现不再依赖 pkg 类型」不是目的，**不成环**才是。判据按顺序：

1. **它自己 import internal 吗？** import = 门面包 ⇒ 只能别名 / 转发 / 极薄适配；
   **不 import = 自包含包** ⇒ 类型真身可以留在 `pkg`（§5.2），`internal` 也**允许** import 它 ——
   这不是例外，是 §5.1 第 4 条明确给出的口子。
2. **倒置的收益是正的吗？** 把纯类型 / 接口的「家」从 `pkg` 搬到 `internal`、
   再让 `pkg` 别名回来，**没有搬动任何实现**，只是多一跳门面，还让业务 F12 从
   「停在 pkg 的类型定义（带契约注释）」变成「停在 alias」——净收益为负。
3. **倒置会不会撞名？** `internal` 侧常已有同名 struct（如 `buff.Container` 接口 vs
   `*buff.Container` 结构、`mob.Mob` 接口 vs `*mob.Mob`、`mover.Mover` 接口 vs `*Mover`、
   `pathfinding.WalkAction` 接口 vs `*WalkAction`）。倒置必须先给那些**接口改名**（加 `Facade`），
   接着 `internal` 的全部实现与测试跟着改 —— 纯名字搬运，且会让**业务可见名的落点**变化。

**本轮（pkg → internal 门面迁移）结论**：`pkg/domain/mmo/{ai/btree,aoi,buff,collide,combat,mapdata,mob,mover,pathfinding,skill,sync}`
**就地保留类型家**（三个判据全部命中：均不 import internal；倒置净收益为负；多数会撞名）。
真正需要迁移的是**带实现体的门面包**：`pkg/app`、`pkg/domain/mmo`、`pkg/domain/object/{gobject,objstore}`
（本轮已全部收敛为别名 / 转发 / 极薄适配，真身在 `internal` 的 `facade.go`）。

**唯一会因迁移产生的环**是「`pkg/<同名>` 变成门面后，`internal/<同名>` 又反向 import 它」；
本轮**没有让它出现**：aoi / 寻路 / buff / skill 的对口包装放在**各自能力的 internal 包**里
（`internal/domain/mmo/{spatial/aoi,spatial/pathfinding,gameplay/buff,gameplay/skill,gameplay/mob}`），
而不是让那 6 个 pkg 子包变成门面。自检：`go build ./...` 零 `import cycle not allowed`。

### pkg 评审口令
拿到一个 `pkg` 包，先跑 `grep -n "github.com/qw576483/clover-server-engine/internal" 该包/*.go`：
- **没命中** ⇒ 自包含包，去看它是不是纯叶子能力（纯工具 / 纯类型 / 纯协议，§3.6）；
- **命中** ⇒ 门面包，逐个导出符号问「这是别名 / 转发 / 1~5 行适配吗？」——
  看到函数体里在遍历、在判断、在建对象、在挂生命周期 ⇒ **落点错了**。

另一方面：业务顺着"自己 new / 持有的东西"往下点，应当能点到门面类型；若撞见 `internal` 类型直接出现在业务签名里，就是**泄漏**（业务根本 import 不到）。

---

## 六、文档与注释规则

1. 目录迁移后，README 必须同步更新。
2. 示例 import 必须指向当前正式路径，不能长期保留旧路径示例。
3. 代码注释中的“门面路径”“实现路径”必须真实有效。
4. 不允许出现“代码迁了，但 README / 注释还是旧世界”的情况。
5. 文档不是可选项；如果目录职责需要解释，就必须写清楚。

### 文档评审口令
代码结构更新但文档没更新，视为这次结构改动未完成。

---

## 七、agent 执行约束

为了防止 agent 偷懒，以后看到下面这些行为，直接视为**未完成**：

1. **只建目录，不迁真实文件**
2. **只改 import，不删旧目录**
3. **只迁代码，不修 README 和注释**
4. **把实现体留在 `pkg` 门面包里**（门面包只许别名 / 转发 / 极薄适配），
   或**同一符号在 pkg 与 internal 各写一份实现**
5. **拿"门面"当挡箭牌**：门面形式本身是**规定动作**（§5.5），但门面指向的真身必须已经在 `internal`；
   不许用门面掩盖「真身还留在 pkg」
6. **写“后续考虑”“视情况处理”这种模糊结论**
7. **把临时落点写成长期正式结构**
8. **不做 grep / build / vet 就宣称完成**
9. **没有明确“做完标准”就说任务结束**

### agent 输出最低要求

任何结构类任务交付时，至少必须说明：

- 改了哪些目录
- 删了哪些旧目录
- 还有没有旧 import 残留
- build / vet 是否通过
- README / 注释是否同步

缺其中任意一项，都不算完整交付。

---

## 八、代码评审提醒

以后评审时，看到下面这些情况，应直接视为结构告警：

1. 在 `pkg` **门面包**里放实现体（在遍历 / 在判断 / 在建对象 / 在挂生命周期），
   或同一符号 pkg 与 internal 各有一份实现
2. 把引擎装配 / 连接管理 / 存储实现放进 `pkg`
3. 新建顶层目录但说不清属于哪 6 类
4. 在 `foundation` 塞运行时原语
5. 在 `domain` 里长横切基础能力
6. 在 `transport` 里写业务决策
7. 在 `shared` 里塞带状态、带生命周期、带框架语义的东西
8. 新增同类并行实现而不是扩展现有能力
9. 创建空目录、占位目录、孤儿实验代码
10. 继续保留同义不同名、同名不同义的脏命名
11. 目录结构改了但 README / 注释 / 示例 import 没跟着改
12. 用“先这样放着”当正式方案

### 结构类 PR 必查清单

评审结构类 PR 时，必须逐项检查：

- [ ] 新增目录是否属于六类之一
- [ ] 是否复用了已有能力，而不是新起一套
- [ ] `pkg` 门面包是否只做别名 / 转发 / 极薄适配（有无实现体残留）；`internal` 有没有反向 import 门面包
- [ ] `pkg` 与 `internal` 之间有无同一符号的双份实现（漂移源）
- [ ] 是否把横切能力错误塞进了 domain
- [ ] 是否把会跑的东西错误塞进了 foundation
- [ ] 是否把业务判断错误塞进了 transport
- [ ] 是否把脏路径留在 README / 注释 / 示例里
- [ ] 是否留下空目录或空壳目录
- [ ] 是否说明了完成标准

---

## 九、一句话执行口令

以后做任何新代码、新目录、新包时，先问自己：

> **它到底属于 app、foundation、runtime、transport、domain、shared 的哪一类？**

如果答不清，先别写。
